% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{lars_volcano}
\alias{lars_volcano}
\title{Volcano plot with targets and significant genes}
\usage{
lars_volcano(
  se,
  contrast,
  id_col = "gene_names",
  target_names = c(""),
  label_sign = TRUE,
  label_targets = TRUE,
  max.overlaps = 15,
  labelsize = 2
)
}
\arguments{
\item{se}{A SummarizedExperiment object containing the gene expression data.}

\item{contrast}{A string specifying the contrast or comparison of interest. This should match the column prefixes for p-values, adjusted p-values, and fold changes in \code{rowData(se)}.}

\item{id_col}{A string specifying the column in \code{rowData(se)} that contains gene IDs or names. Defaults to "gene_names".}

\item{target_names}{A character vector of target gene names to be highlighted. Defaults to an empty vector.}

\item{label_sign}{A logical value indicating whether significant genes should be labeled on the plot. Defaults to TRUE.}

\item{label_targets}{A logical value indicating whether target genes should be labeled on the plot. Defaults to TRUE.}

\item{max.overlaps}{Maximum number of label overlaps allowed for \code{ggrepel}. Defaults to 15.}

\item{labelsize}{Numeric value for the text size of labels. Defaults to 2.}
}
\value{
A ggplot2 object representing the volcano plot.
}
\description{
This function creates a volcano plot based on the input summarized experiment (SE) object, highlighting significant genes and specific target genes.
}
\details{
The volcano plot shows the log2 fold changes on the x-axis and the -log10 p-values on the y-axis. Significant genes and target genes are highlighted with different colors and shapes. Optionally, labels can be added for significant and target genes using \code{ggrepel}.
}
\examples{
# Example usage:
# volcano_plot <- lars_volcano(se, contrast = "condition_A_vs_B", target_names = c("gene1", "gene2"))
# print(volcano_plot)

}
